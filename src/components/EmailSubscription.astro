---

---

<section
  id="newsletter"
  class="py-20 bg-gradient-to-r from-[#f0fdfa] to-[#eff6ff]"
>
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <div
      id="newsletter-card"
      class="bg-white rounded-3xl shadow-xl p-8 md:p-12 transition-shadow duration-300 hover:shadow-2xl"
    >
      <!-- Message When you subscribe -->
      <div id="success-message" class="hidden">
        <div class="text-green-500 text-6xl mb-4" aria-hidden="true">✓</div>
        <h3 class="text-2xl font-bold text-slate-800 mb-2">
          ¡Gracias por suscribirte!
        </h3>
        <p class="text-slate-600 text-lg">
          Pronto recibirás nuestro boletín con consejos y promociones
          exclusivas.
        </p>
      </div>

      <div id="form-container">
        <div class="text-teal-500 text-6xl mb-6" aria-hidden="true">💌</div>
        <h2 class="text-3xl md:text-4xl font-bold text-slate-700 mb-4">
          Mantente al Día con Consejos para tu Mascota
        </h2>
        <p class="text-lg text-slate-600 mb-8 max-w-2xl mx-auto">
          Suscríbete a nuestro boletín y recibe promociones, consejos de cuidado
          y noticias de salud directamente en tu correo.
        </p>

        <form id="newsletter-form" class="max-w-md mx-auto">
          <div class="flex flex-col gap-4">
            <input
              type="email"
              placeholder="Ingresa tu correo electrónico"
              class="flex-1 px-6 py-4 border border-slate-300 rounded-full focus:outline-none focus:ring-1 focus:ring-teal-400 focus:border-transparent text-md text-slate-700"
              required
              aria-label="Correo electrónico para suscripción"
            />
            <button
              type="submit"
              class="bg-teal-600 hover:bg-teal-700 text-white px-8 py-4 rounded-full font-semibold text-md transform hover:scale-105 transition-all duration-300 whitespace-nowrap focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-teal-600"
            >
              Suscribirme Ahora
            </button>
          </div>
        </form>
      </div>

      <!-- list of benefits -->
      <ul
        class="mt-8 grid grid-cols-1 md:grid-cols-2 gap-6 text-sm text-slate-500"
      >
        <li class="flex items-center justify-center">
          <span class="mr-2" aria-hidden="true">🎯</span>
          Promociones exclusivas
        </li>
        <li class="flex items-center justify-center">
          <span class="mr-2" aria-hidden="true">📚</span>
          Consejos de cuidado
        </li>
      </ul>
    </div>
  </div>
</section>

<script is:inline>
  // Script para manejar la suscripción de forma interactiva
  document.addEventListener("DOMContentLoaded", () => {
    const formContainer = document.getElementById("form-container");
    const successMessage = document.getElementById("success-message");
    const newsletterForm = document.getElementById("newsletter-form");

    // Chequeo si el usuario esta subscrito (usando localStorage)
    if (localStorage.getItem("isSubscribedToNewsletter") === "true") {
      formContainer.classList.add("hidden");
      successMessage.classList.remove("hidden");
    }

    //Si no esta subscrito, mostramos el formulario
    newsletterForm.addEventListener("submit", (e) => {
      e.preventDefault();

      // Ocultamos el formulario y mostramos el mensaje de éxito
      formContainer.classList.add("hidden");
      successMessage.classList.remove("hidden");

      //Luego lo guardamos en localStorage por si recarga la pagina xd
      localStorage.setItem("isSubscribedToNewsletter", "true");
    });
  });
</script>
